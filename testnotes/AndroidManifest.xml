<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.ht.testnotes"
          android:versionCode="1"
          android:versionName="1.0"
        >

    <uses-sdk android:minSdkVersion="7"/>
    <application
            android:label="test便笺"
            android:icon="@drawable/icon"
            >
        <activity
                android:name=".activities.SplashActivity"
                android:theme="@android:style/Theme.NoTitleBar"
                >
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activities.LoginActivity"
                  android:theme="@android:style/Theme.NoTitleBar"
                />
        <activity android:name=".activities.MainActivity"
                  android:theme="@android:style/Theme.NoTitleBar"
                />
        <activity android:name=".activities.RegisterActivity"
                  android:theme="@android:style/Theme.NoTitleBar"
                />
        <activity android:name=".activities.AddActivity"
                  android:theme="@android:style/Theme.NoTitleBar"
                />
        <activity android:name=".activities.EditActivity"
                  android:theme="@android:style/Theme.NoTitleBar"
                />

    </application>
    <uses-permission android:name="com.ht.mynote.contentprovider.MyContentProvider"></uses-permission>
</manifest>
